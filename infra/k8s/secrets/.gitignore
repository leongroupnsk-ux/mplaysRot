# Never commit real Kubernetes Secrets — they contain base64-encoded credentials.
# Use app-secrets.yaml.example as a template, fill in values, and apply directly:
#   kubectl apply -f infra/k8s/secrets/app-secrets.yaml
# Or manage secrets via External Secrets Operator / Vault Agent.
*.yaml
!*.example
